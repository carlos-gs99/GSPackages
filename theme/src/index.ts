// ==========================================
// THEME INDEX
// ==========================================

// Note: CSS tokens são importados diretamente via import '@globalsoft/theme/tokens.css'

// Re-export types
export type Theme = 'light' | 'dark';
export type Palette = 'blue' | 'green' | 'purple' | 'orange' | 'red';

